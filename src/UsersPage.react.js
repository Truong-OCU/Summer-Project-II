// @flow

import * as React from "react";
import { Page, Grid, GalleryCard, Form } from "tabler-react";
import SiteWrapper from "./SiteWrapper.react";

function UsersPage() {
  return (
    <SiteWrapper>
		<Page.Content title="This is the User Page">
		</Page.Content>
    </SiteWrapper>
  );
}

export default UsersPage;
